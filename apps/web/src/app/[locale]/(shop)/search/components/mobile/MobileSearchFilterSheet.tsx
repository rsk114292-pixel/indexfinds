'use client';

/**
 * Re-export MobileFilterSheet for search page usage.
 * Previously a duplicate — now shares the same accordion-style component.
 */
export { MobileFilterSheet as MobileSearchFilterSheet } from '../../../products/components/mobile/MobileFilterSheet';
export type { MobileFilterSheetProps as MobileSearchFilterSheetProps } from '../../../products/components/mobile/MobileFilterSheet';
